-- ============================================================
-- MOS Shift App - 管理者による時間調整フラグの追加
-- ============================================================
-- 管理者がスタッフの希望時間を変更して承認した場合に
-- admin_adjusted = true をセットする。
-- スタッフ側でこのフラグを検知して「店長により調整されました」と表示する。
-- ============================================================

ALTER TABLE public.shifts
  ADD COLUMN IF NOT EXISTS admin_adjusted boolean NOT NULL DEFAULT false;
